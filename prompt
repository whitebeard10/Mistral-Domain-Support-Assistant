You are a senior ML engineer helping me build a realistic, technically credible LLM fine-tuning project for my GitHub portfolio.

The goal is NOT to create a toy chatbot demo. The repository should look like a serious junior-to-mid-level applied LLM engineering project focused on efficient fine-tuning, evaluation rigor, inference optimization, and reproducibility.

Build the project in Python using:

- PyTorch
- Hugging Face Transformers
- PEFT
- QLoRA
- bitsandbytes
- Accelerate
- TRL
- Weights & Biases (optional but preferred)

========================
PROJECT OVERVIEW
================

Project Name:
mistral-domain-support-assistant

Goal:
Fine-tune Mistral-7B-Instruct for a domain-specific support / FAQ assistant using QLoRA on a limited hardware setup (single 16GB GPU).

The project should demonstrate:

- parameter-efficient fine-tuning
- low-memory training
- evaluation methodology
- inference optimization
- practical engineering tradeoffs

NOT:

- research-grade training
- synthetic AGI claims
- benchmark inflation

========================
MODEL & TRAINING SETUP
======================

Base Model:

- Mistral-7B-Instruct

Fine-Tuning Method:

- QLoRA
- 4-bit quantization
- PEFT adapters

Training Configuration:

- LoRA rank (r): 16
- LoRA alpha: 32
- target modules: q_proj, v_proj
- bf16 where supported
- gradient checkpointing enabled
- gradient accumulation enabled
- paged AdamW optimizer
- sequence length truncation handling

Use:

- bitsandbytes quantization
- Hugging Face Trainer or SFTTrainer
- Accelerate for memory-efficient training

========================
DATASET REQUIREMENTS
====================

Dataset:

- ~4,000 instruction-response pairs
- domain-specific support dataset
- realistic FAQ / troubleshooting / operational assistant use case

Implement:

- dataset cleaning
- duplicate removal
- prompt formatting
- train/validation split
- token length analysis
- truncation safeguards

Prompt format: <system> <instruction> <response>

Generate realistic preprocessing pipeline scripts.

========================
TRAINING PIPELINE
=================

Build modular scripts for:

- dataset preprocessing
- tokenizer setup
- model loading
- QLoRA configuration
- training
- evaluation
- inference benchmarking

Track:

- training loss
- validation loss
- GPU memory usage
- tokens/sec
- epoch duration

Apply:

- early stopping at epoch 3 after identifying overfitting onset from validation loss divergence

Document:

- why early stopping was necessary
- tradeoff between specialization vs overfitting

========================
EVALUATION
==========

Implement evaluation workflow using:

- ROUGE-L
- BLEU
- qualitative comparison
- zero-shot baseline comparison

Expected outcome:

- ~22% ROUGE-L improvement over zero-shot baseline

Generate:

- evaluation scripts
- markdown evaluation report
- before vs after examples
- failure cases

Include realistic limitations:

- hallucination persistence
- weak multi-turn memory
- sensitivity to ambiguous instructions
- domain over-specialization

========================
INFERENCE OPTIMIZATION
======================

Optimize inference for:

- single 16GB GPU deployment

Implement:

- bitsandbytes quantized inference
- model loading optimizations
- max_new_tokens safeguards
- batching experiments
- latency benchmarking

Expected target:

- ~2.4s average response latency

Measure:

- cold-start latency
- average generation latency
- memory consumption

========================
FASTAPI INFERENCE API
=====================

Build lightweight inference API with:

- FastAPI
- /generate endpoint
- async support
- structured JSON logging
- request validation
- configurable generation parameters

Return:

- response
- token count
- latency
- generation metadata

========================
GITHUB REQUIREMENTS
===================

Generate:

- production-style folder structure
- requirements.txt
- .env.example
- Dockerfile
- README.md

README must include:

- architecture overview
- QLoRA explanation
- memory optimization strategy
- training configuration
- evaluation metrics
- benchmark tables
- training curves
- inference optimization notes
- limitations and tradeoffs

Add Mermaid architecture diagrams for:

- training pipeline
- inference flow

========================
CODE QUALITY
============

Requirements:

- modular architecture
- type hints
- realistic engineering comments
- clean separation of concerns
- reproducible training setup
- configuration-driven design

Avoid:

- notebook-only implementation
- giant monolithic scripts
- tutorial-style code structure
- fake enterprise claims

========================
IMPORTANT
=========

The final repository should look like:

- a serious applied LLM engineering project
- built by someone transitioning from backend engineering into AI engineering

NOT:

- a hype-driven “AGI startup” demo
- a copy-pasted tutorial repo

Prioritize:

- reproducibility
- engineering realism
- memory optimization
- evaluation rigor
- operational tradeoffs
  over flashy AI terminology.
